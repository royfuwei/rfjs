import { HomeDefaultView } from '../../sections/home/view';

export const metadata = {
  title: 'The starting point for your next project',
};

export default function HomePage() {
  return <HomeDefaultView />;
}
